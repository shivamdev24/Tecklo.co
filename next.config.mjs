/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.datocms-assets.com",
      "images.unsplash.com",
      "img.freepik.com",
    ],
  },
};

export default nextConfig;
